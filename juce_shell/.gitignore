# CMake build trees + plugin artefacts (段E: never commit build output).
# One pattern for every tree: build/ (default), build-universal/ (B-082 ship build),
# build-pdc-macos/ (host validation), build-linux/ and build-windows/ (the per-platform
# Reference preview renderer trees that hyphaReferencePreviewLocations.cjs looks in).
build*/
