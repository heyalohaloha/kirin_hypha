# Kirin OS 設計制約（非交渉原則）

**正本責任:** 運用担当  
**最終更新:** 2026-05-08（R-28 判定基準再定義 /   
**Hypha適用版:** 2026-05-13  
**適用:** Claude Code（CLAUDE.md経由で参照）/ kirin_hypha  
**優先順位:** 本ファイルが正本。CLAUDE.mdと矛盾した場合は本ファイルを優先する。  
**Lens正本との関係:** 共通原則は同一。Hypha固有注記は `[Hypha]` タグで明示する。

-----

## 約束5原則（ADVISOR・観察層の振る舞い原則）

1. **設定値の読み取りに徹する** — プラグイン内部アルゴリズムに立ち入らず、ユーザー設定値の範囲内で情報活用する。
1. **測定結果を的確に分析する** — 音楽理論・製作知見を活かし、設定値から適切なアドバイスを導く。
1. **ユーザーの判断を最大限尊重する** — 提案はするが最終決定権はユーザー。押し付け・強制は厳禁。
1. **プラグインベンダーとの良好な関係を保つ** — 開発意図を丁寧に説明。ベンダーの意向もできる限り尊重する。
1. **ユーザーファーストの精神を貫く** — 常にユーザーの創造性を後押しする。機能・UIはそれに沿って設計する。

-----

## アーキテクチャ原則

**ZSA（Zero Speculation Architecture）**  
存在するものだけを計測する。推測を事実として提示しない。

**R-12（Manufacturing Boundary）**  
Kirinはオーディオ信号を生成・変更しない。データアーティファクトのみを出力する。  
**[Hypha] Audio Thread 制約:** nih-plug の `process_audio` コールバック（Audio Thread）では、信号処理以外の余計な実行（ファイルI/O・ロック待ち・アロケーション等）を行わない。計測・表示・ファイルI/Oは Audio Thread 外で実施する。プラグインパラメータの書き込みは「音声信号の変更」には該当しないが、Audio Thread 内での I/O を引き起こす実装は禁止する。

**R-13（Hub & Spoke）**  
`work.json`（schema: `work.schema.json`）が全システムの接続点。各モジュール間はこの接続点を通じて連携する。売り切り。サーバー・アカウント不要。  
**Phase 1 では plugin_data ディレクトリファイル投函方式を正本とする。work.json 完全準拠は Phase 2 で再定義予定。**

**分離原則（Separation Principle）**  
エンジン / ADVISOR / UI / ファイル管理 / セクション検出 は独立動作。1モジュールの失敗が他にカスケードしてはならない。  
**[Hypha] 3層隔離:** Audio Thread（バッファコピーのみ / 絶対に落ちない）/ Measure Thread（計測 / クラッシュ → Audio Thread が検出 → 自動再起動）/ IO Thread（/tmp/ 書き込み / クラッシュ → 同様に自動再起動）。Audio Thread が止まる = DAW の再生が止まる = 利用者の作業が全壊。この優先順位は絶対。

-----

## フェーズ分類

- **Phase 1 / Phase 2 のみ。** 他のフェーズ名称（Phase 3 / Alpha / Beta 等）は使わない。
- Phase 2送りはDaisukeの承認なしに決定しない。
- **Loop Phase 0-7 は別軸:** 内部仕様書 に定める「1ループ体験段階」を指し、本項「Phase 1/2 のみ」のリリースフェーズとは別軸である。

-----

## 品位原則（変更不可 — 2026-03-31確定）

他者に言及しない。以下を全て禁止：

- 競合名の明示
- 間接比較（「他社製品より〜」等）
- 優位主張（「業界初」「最高精度」等）
- 排他的表現（「これしかない」等）

UI・ADVISOR出力・販売文言・Guardian文書を含む全ての場所に適用。

-----

## 精度原則

**R-27（精度優先）**  
精度を速度に優先する。プロは正確さに時間を投資する。リリース日は目安であり精度に劣後する。

-----

## 価値判断原則

**R-7（価値判断禁止 / 全体適用）**  
Kirin OSはユーザーに対する価値判断を出さない。計測事実のみを提示する。UI・ADVISOR出力・レポート・ドキュメント全てに適用。ADVISORへの具体化がR-22。

-----

## UI制約

**[Hypha] egui GUI に適用する。**

**禁止色**  
以下の色を使わない：

- 赤（#c44含む全ての赤系）
- ネオン系
- 純白（#ffffff）— Dark Cockpit違反（G-72-10確定）

TP超過時は琥珀の明度のみ上昇させる。色相は変えない。

**Dark Cockpit**  
重要なものだけが点灯する。不要な情報を常時表示しない。

**アニメーション禁止**  
静止画基本。アニメーション・点滅・グローを使わない。

**Biophilic**  
環境型のみ。報酬型（ガミフィケーション・達成バッジ等）は使わない。

-----

## ADVISOR制約

**R-22**  
ADVISORは価値判断を出さない。全曲対象。（R-7のADVISOR具体化）

**R-26（沈黙ゲート）**  
ADVISORは言うことがなければ沈黙する。

**R-28（機能的沈黙）**  
内部検証および互換 fallback 等で利用者操作と非紐づきの失敗は無言で skip する。UIにエラーを出さない。ただし利用者が明示意図して操作した結果の失敗（沈黙すれば「問題なく進んだ」と勘違いするケース）は通知必須。

-----

## 作業規律（Claude Code・開発部屋必読）

以下は「原則」ではなく「作業規律」。設計制約として全件守ること：

- **R-9:** 入力データを1件も見ずにコードを書かない
- **R-10:** 出力の妥当性を数字で確認する
- **R-11:** 外部ツールのコマンド/APIは公式ドキュメントで確認する
- **正常系だけテストしない:** エラーパス・境界値もテストする
- **既存コードを読んでから実装:** 特に計測コア・SignalState 等
- **1モジュールだけ修正して他を放置しない:** 同種の問題は全対象モジュールを一括処理
- **vendor/* 配下の clippy 警告は監査対象外**（upstream 修正待ち）。kirin_hypha 本体の警告ゼロを品質基準とする

-----

## R番号一覧（正本 / 2026-05-08 

### 設計制約収録済み

| R# | 定義 |
|---|---|
| R-7 | 全体価値判断禁止 — Kirin OSはユーザーに対する価値判断を出さない。計測事実のみを提示する |
| R-9 | 作業規律: 入力データを1件も見ずにコードを書かない |
| R-10 | 作業規律: 出力の妥当性を数字で確認する |
| R-11 | 作業規律: 外部ツールのコマンド/APIは公式ドキュメントで確認する |
| R-12 | Manufacturing Boundary — Kirin OSは音声を加工しない。データアーティファクトのみを出力する。**[Hypha] Audio Thread では信号処理以外の実行禁止。書込機能は Phase 2 送り。** |
| R-13 | Hub & Spoke — work.json（schema: work.schema.json）が全システムの接続点。売り切り。サーバー・アカウント不要。**Phase 1 では plugin_data ディレクトリファイル投函方式を正本とする。work.json 完全準拠は Phase 2 で再定義予定。** |
| R-22 | ADVISOR価値判断禁止 — R-7のADVISOR具体化。全曲対象 |
| R-26 | 沈黙ゲート — ADVISORは言うことがなければ沈黙する |
| R-27 | 精度優先 — リリース日は目安であり精度に劣後する |
| R-28 | 機能的沈黙 — 内部検証等の利用者操作非紐づき失敗は無言でskip。UIにエラーを出さない。ただし利用者が明示意図して操作した結果の失敗（沈黙で「問題なく進んだ」と勘違いするケース）は通知必須。 |

### Phase 2送り（設計制約に収録しない）

| R# | 内容 | 出典 |
|---|---|---|
| R-14 | Dolby Atmos対応 |  |
| R-15 | ADM BWFメタデータ読み取り |  |
| R-16 | Phase 2 = Atmosラウドネス計測 |  |
| R-17 | DDEX ERN 4.3 SoundRecordingEdition方式 |  |
| R-18 | channel_configurationは拡張可能オブジェクト |  |
